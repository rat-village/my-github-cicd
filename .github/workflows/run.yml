name: Annotation
on: push
jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::This is an error"
          echo "::error::This is an error2"
          echo "::warning::This is a warning"
          echo "::notice::This is a notice"
